const config = {
    status: [
        { label: "job", value: false },
        { label: "启用", value: true }
    ]
}

const jobReducer = function (state = config, action) {
    return state;
}

export default jobReducer;